FROM node:20-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies
RUN npm install

# Copy source code
COPY . .

# Expose the port
EXPOSE 5173

# Start the app
# Use --host to expose to the network
CMD ["npm", "run", "dev", "--", "--host"]
